# course-dashboard

课程数据看板（dashboard）Web 应用。

**技术栈**：Next.js 14 + TypeScript、Tailwind CSS、Radix UI、lucide-react。

## 本地运行

```bash
npm install
npm run dev        # 开发服务器 (http://localhost:3000)
npm run build      # 生产构建
npm start          # 运行生产构建
```
